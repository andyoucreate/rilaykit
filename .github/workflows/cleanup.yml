name: Cleanup

on:
  schedule:
    # Run every Sunday at 2 AM UTC
    - cron: '0 2 * * 0'
  workflow_dispatch:

jobs:
  cleanup-artifacts:
    name: Cleanup Old Artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Cleanup old artifacts
        uses: actions/github-script@v7
        with:
          script: |
            const artifacts = await github.rest.actions.listArtifactsForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              per_page: 100
            });

            const cutoff = new Date();
            cutoff.setDate(cutoff.getDate() - 30); // Keep artifacts for 30 days

            for (const artifact of artifacts.data.artifacts) {
              const createdAt = new Date(artifact.created_at);
              if (createdAt < cutoff) {
                console.log(`Deleting artifact: ${artifact.name} (${artifact.created_at})`);
                await github.rest.actions.deleteArtifact({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  artifact_id: artifact.id
                });
              }
            }

  cleanup-packages:
    name: Cleanup Old Package Versions
    runs-on: ubuntu-latest
    if: github.repository_owner == 'andyoucreate' # Only run on main repo
    steps:
      - name: Cleanup old prerelease versions
        uses: actions/github-script@v7
        with:
          script: |
            // This would require npm API access to clean old prereleases
            // For now, just log what we would do
            console.log('Would cleanup old prerelease versions older than 60 days');
            console.log('This requires npm API integration which should be done carefully'); 